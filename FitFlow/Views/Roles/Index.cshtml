@model IEnumerable<FitFlow.Models.Entities.Role>

@{
    ViewData["Title"] = "Roles";
}

<h1>Roles</h1>

<p>
    <a asp-action="Create" class="btn btn-primary bi bi-plus-circle"> Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RoleName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoleName)
            </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.RoleID" class="btn btn-primary btn-sm bi bi-file-earmark-text"> Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.RoleID" class="btn btn-secondary btn-sm bi bi-card-text"> Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.RoleID" class="btn btn-danger btn-sm bi bi-trash"> Delete</a>
                </td>
        </tr>
}
    </tbody>
</table>
